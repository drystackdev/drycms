# build output
dist/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# drycms local file storage (see `storage.root` in dry.config.ts)
/storage/

# drycms Icon Management's own local file storage (see `icons.root` in dry.config.ts)
/icons/
/content/
/richtext-components/

# Playwright artifacts (unanchored - a command run from the wrong cwd can
# create these under a subdirectory instead of the repo root)
test-results/
playwright-report/

# default `content.engine: "sqlite"` database file (see `dry.config.ts`)
*.sqlite

Sortable-master
drystack
.qa-tmp3
e2e
.codegraph
/kv/