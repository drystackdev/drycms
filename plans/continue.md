[x] (phần "select all") khi forcus vào 1 component inline thì luôn select all text trong compoent đó - đã fix: `dry-component-view.ts` chỉ tạo `NodeSelection` khi mousedown KHÔNG rơi vào 1 element tương tác thật (button/input/link/...) bên trong component, dùng `event.composedPath()` để không bị shadow-DOM retarget đánh lừa. Đã verify bằng Playwright (Carousel's prev/next button giờ bấm được bình thường, click vào phần không tương tác vẫn select node như cũ).
[x] (phần "build nhỏ gọn cho production") - đã xác nhận ý: giảm kích thước file .js mỗi component. Nguyên nhân: build component (`build-component-bundle.ts`) chỉ chạy bên trong dev server (không có bước build riêng cho production), nên nó kế thừa `NODE_ENV=development` của dev server -> Vite bật JSX dev-transform (`__source`/`fileName`/`lineNumber` gắn vào mọi element) + Preact DevTools hook + tắt minify mặc định. Đã fix: ép `NODE_ENV=production` khi build (an toàn với build đồng thời qua 1 counter), tắt `devToolsEnabled`/`prefreshEnabled`, set `minify: "oxc"` tường minh. Carousel.js giảm 5729 -> 4189 bytes (~27%), color-text.js giảm 4286 -> 3431 bytes (~20%). Verify bằng Playwright (rebuild qua API + insert lại vào editor, next/prev vẫn hoạt động).

[x] icons và richtext-components cần đi chung với storage nhưng lưu khác thư mục (giống content) - ĐÃ có sẵn từ trước (`richtext.storage`/`icons` trong `options.ts`, cùng dạng local/github/gitlab như `storage`/`content`, root riêng).
[x] hệ thống thường xuyên lỗi `Component error: Cannot read properties of undefined (reading '__H')` ở component - đây LÀ lỗi thật (không phải dev sync), xảy ra chắc chắn với bất kỳ component đã confirm nào có dùng hooks (vd Carousel's `useState`): bundle build-riêng của mỗi component share 1 "preact.js" vendor bundle riêng biệt với bản `preact` mà `dry-component-runtime.ts` tự import - 2 instance Preact khác nhau nên hooks nội bộ (`currentComponent`) không bao giờ được set khi host render. Đã fix: `dry-component-runtime.ts` giờ mount qua đúng instance Preact mà bundle của component đó dùng (fetch `preact.js` cùng gốc). Verify bằng Playwright: Carousel dùng `useState` không còn crash, next/prev hoạt động bình thường.
[x] (phần "bỏ JSON output") - đã bỏ `json`/`onJsonChange` khỏi `RichTextFieldProps`/`useRichTextEditor`, editor giờ chỉ seed/output HTML. Đã dọn theo: `Showcase.tsx` (bỏ panel "Output (JSON)"), `mock/showcase.ts` (code sample tương ứng), `RichTextDemo.tsx` (import chết). Verify bằng Playwright + tsc + test suite.
[ ] (phần "field trong content type + settings") - CHƯA làm, để sau theo yêu cầu.
[x] fullScreen tong richtext cần hiện thêm Label, và description nếu có, cần css cho toolbar có border-radius ở chế độ fullScreen này - đã thêm `.richtext-fullscreen-header` (label+description) hiện khi fullscreen, và `.richtext-toolbar` có `border-radius` riêng trong chế độ này. Verify bằng Playwright screenshot.

[x] Tất cả các Field input khi đang nhập cần 1 cơ chế bắt ra event cho bên khác lắng nghe... - đã làm, phạm vi: TẦNG FieldRenderer/entry-editor (trang entry+singleton), KHÔNG sửa từng field component riêng lẻ (đã hỏi & xác nhận). Chi tiết:
  - File mới `content-entry-editor/field-events.ts`: `dispatchFieldInput` (bắn `window` CustomEvent `dry:field-input`, debounce 300ms/field), `listenForFieldSet` (nghe `dry:field-set` để GHI giá trị từ ngoài vào, luôn ghi đè kể cả đang focus - theo yêu cầu), `scrollToField` (`?_field=` -> scrollIntoView center + class `entry-field-highlight` tự xoá sau 1.5s, CSS animation fade trong components.css).
  - `ContentEntryEditor.tsx`: mỗi field top-level (trái+phải, kể cả cặp Title+Slug) giờ có wrapper `data-field-name={fieldName}` (dùng chung 1 điểm `renderFieldNodes`, không đụng 16 field component riêng). `updateFieldValue` bắn `dry:field-input` mỗi lần đổi; effect nghe `dry:field-set`; effect đọc `?_field=` (qua `useParam` có sẵn) để scroll+highlight sau khi form load xong.
  - LƯU Ý phạm vi: chỉ field TOP-LEVEL của entry/singleton (không bao gồm field lồng trong `flatten`/component lặp lại - không có 1 vị trí DOM cố định để đặt name/scroll cho các trường hợp lặp).
  - Verify bằng Playwright: `dry:field-input` bắn đúng debounce+payload (name/value/typeSlug/entryId), `dry:field-set` ghi đúng vào field, `?_field=url` scroll+highlight đúng field.

[x] các field phải được lòng nhau nested array thì có thể ghi `data.0.name.label` - đã làm: file mới `content-entry-editor/field-path.ts` (`setValueAtPath`, immutable, split theo dấu `.`, segment toàn số = index mảng) - field name không bao giờ chứa dấu `.` (`naming.ts`) nên tách path không mơ hồ. `dry:field-set` (`ContentEntryEditor.tsx`'s `applyFieldSet`) giờ chạy `name` qua hàm này thay vì spread top-level cũ; field top-level tự nó (mỗi field's own `onChange`, qua `updateFieldValue`) KHÔNG đổi - luôn gọi với tên phẳng, không cần path-parse mỗi phím gõ. Path sai (index tràn mảng, index vào không phải mảng, key vào không phải object) -> `console.warn` + no-op, không throw. GIỚI HẠN đã biết: nếu item đang mở trong dialog của `ComponentField` (component-repeat), ghi vào `value` đúng nhưng dialog's `draft` cục bộ không tự cập nhật cho tới khi đóng/mở lại - cùng giới hạn "không có vị trí sống cố định" mục 13 đã nêu. Verify: 9 unit test mới (`field-path.test.ts`) + `tsc` (`build:lib`) pass; suite tổng 617/618 pass (1 fail còn lại ở `seed.test.ts` không liên quan, không đụng tới).
[x] Richtext khi tôi bật inline thì html output là nội dung innerHTML của thẻ p không bao gồm cả thẻ p - đã làm: `exportCleanHtml(doc, { inline })` (`html.ts`) tái dùng nguyên `containerContentHtml` sẵn có (logic y hệt `<li>`/table cell: đúng 1 paragraph phẳng -> chỉ `inlineChildrenHtml`, không wrapper; nhiều block -> fallback export đầy đủ như cũ, không mất nội dung). `useRichTextEditor.ts` nhận thêm `inline` (qua `inlineRef` như `onChangeRef`, vì effect mount chỉ chạy 1 lần), `RichTextField.tsx` giờ truyền `inline` xuống (trước đây chỉ tới toolbar). Không cần đổi gì phía import - `blockChildrenFromContainer` vốn đã tự bọc inline HTML trần vào 1 paragraph. Verify bằng Playwright trên `/dry/richtext-demo`: bật Inline + gõ chữ -> output không còn `<p>`; tắt Inline -> có `<p>` lại; load preset nhiều block khi đang Inline -> fallback export đầy đủ, không crash.



