# AI hỗ trợ viết bài cho entry hoặc single tone

- Khi vào 1 trang nhập liệu nhập entry hoặc nhập thông tin singleton
- AI có thể hiện bảng trắc nghiệm (1 / nhiều lựa chọn) để tương tác với người dùng khi thạt sự cần thiết

# Có nút AI generate có label là `Magic Write`

- mở dialog để nhập prompt viết bài
- chọn hình ảnh từ file management gửi lên AI (giảm dung lượng ảnh nhỏ lại để AI đủ hiểu nội dung ành để dùng ảnh phù hợp - dùng ảnh dựa trên path của ảnh)
- Viết phù hợp cho từng loại field đang có trong trang - hiểu được tất cả - có thể lấy all data của ref nếu cần (bổ xung hàm cho AI nếu cần) - viết Richtext có thể tận dụng được tất cả chức năng từ grid, table,... và image(đã chọn từ trước)
- AI có thể điền thêm dữ liệu còn giang dở, hoặc ghi đề 1 hoăch nhiều tuỳ vào câu prompt nhập vào
- trong richtext có thể chọn 1 đoạn và dùng AI đẻ thay đổi đoạn đó theo ý