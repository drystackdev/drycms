# Plan

- Seed a default `Main` menu with Home and About items when the built-in menu schema is first created.
- Add a starter root layout that reads `menu` through `dry()` and renders navigation.
- Add a persisted light/dark toggle and an About starter page.
- Verify seed tests, page-source compilation, and TypeScript.

# Status

- Seed, starter layout, About page, and theme toggle implemented.
- Targeted seed/pages-source tests and TypeScript typecheck pass.

# Speed

- Complete. No blockers.
